# Create a color_codes variable and assign a tuple to it. The tuple
# should contain three tuples as items. The three tuples can contain
# any type of data inside them. In other words, your variable should
# be equal to a tuple of tuple of three items each.

a = ('one', 'two', 'three')
b = (1, 2, 3)
c = (0.5, 1.0, 1.5)
color_codes = (a, b, c)